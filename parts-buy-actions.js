/* Compra de peças do almoxarifado: botão visível em cada card + persistência no Firebase + entrega. */
(function(){
'use strict';
const DELIVERY_MS=30000;
function db(){try{return (typeof firebase!=='undefined'&&firebase.database)?firebase.database():null}catch(e){return null}}
function partsRef(){const d=db();return d?d.ref('workshopCMMS/parts'):null}
function ordersRef(){const d=db();return d?d.ref('workshopCMMS/partOrders'):null}
function value(p,...keys){for(const k of keys){if(p&&p[k]!==undefined&&p[k]!==null&&p[k]!=='')return p[k]}return ''}
function partName(p,id){return String(value(p,'name','nome','partName','descricao','description','code','codigo')||id||'Peça')}
function partQty(p){const n=Number(value(p,'quantity','stock','qty','quantidade')||0);return Number.isFinite(n)?n:0}
function entries(parts){return Object.entries(parts||{})}
function formatRemaining(ms){const s=Math.max(0,Math.ceil(ms/1000));if(s<60)return s+'s';const m=Math.floor(s/60),r=s%60;return m+'min'+(r?' '+r+'s':'')}
function addStyles(){if(document.getElementById('partsBuyStyles'))return;const s=document.createElement('style');s.id='partsBuyStyles';s.textContent=`.parts-buy-btn{width:100%;margin-top:8px;padding:9px 12px;border-radius:10px;background:#2563eb;border:1px solid #3b82f6;color:#fff;font-size:12px;font-weight:800;cursor:pointer;display:block!important;visibility:visible!important;opacity:1!important;position:relative;z-index:2}.parts-buy-btn:hover{background:#3b82f6}.parts-buy-btn:disabled{opacity:.65!important;cursor:wait}.parts-buy-box{width:100%;box-sizing:border-box;margin-top:8px;padding:10px;border-radius:10px;background:#0f172a;border:1px solid #334155;display:none}.parts-buy-box.open{display:block}.parts-buy-box input{width:100%;box-sizing:border-box;padding:8px 10px;border-radius:8px;background:#020617;border:1px solid #334155;color:#e2e8f0}.parts-buy-box .pbb-actions{display:flex;gap:7px;margin-top:8px}.parts-buy-box button{border:0;border-radius:8px;padding:8px 10px;font-weight:800;cursor:pointer}.pbb-cancel{background:#1e293b;color:#e2e8f0}.pbb-confirm{background:#2563eb;color:#fff;flex:1}.parts-delivery-status{width:100%;box-sizing:border-box;margin-top:8px;padding:8px 10px;border-radius:9px;background:#172554;border:1px solid #1d4ed8;color:#bfdbfe;font-size:11px;font-weight:800}.parts-delivery-status.received{background:#052e16;border-color:#166534;color:#86efac}`;document.head.appendChild(s)}
async function loadParts(){const r=partsRef();if(!r)return {};try{const s=await r.once('value');return s.val()||{}}catch(e){console.error('Erro ao ler peças:',e);return {}}}
async function loadOrders(){const r=ordersRef();if(!r)return {};try{const s=await r.once('value');return s.val()||{}}catch(e){console.error('Erro ao ler pedidos:',e);return {}}}
function findEntry(card,parts,index){const es=entries(parts);const text=(card.textContent||'').trim().toLowerCase();return es.find(([id,p])=>{const n=partName(p,id).trim().toLowerCase();return n&&text.includes(n)})||es[index]}
async function createPurchase(id,p,q){const pr=partsRef(),or=ordersRef();if(!pr||!or)throw new Error('Firebase indisponível');const now=Date.now(),eta=now+DELIVERY_MS;const orderRef=or.push();const order={partId:id,partName:partName(p,id),quantity:q,unit:value(p,'unit','unidade')||'un',status:'pending',createdAt:firebase.database.ServerValue.TIMESTAMP,etaAt:eta,deliverySeconds:DELIVERY_MS/1000};await orderRef.set(order);await pr.child(id).update({lastPurchaseAt:firebase.database.ServerValue.TIMESTAMP,lastPurchaseQuantity:q,lastPurchaseOrderId:orderRef.key,lastPurchaseStatus:'pending',lastPurchaseEta:eta});return orderRef.key}
async function deliverOrder(orderId,order){const or=ordersRef(),pr=partsRef();if(!or||!pr||!order||order.status!=='pending'||Number(order.etaAt)>Date.now())return false;const tx=await or.child(orderId).transaction(cur=>{if(!cur||cur.status!=='pending'||Number(cur.etaAt)>Date.now())return cur;return Object.assign({},cur,{status:'delivered',deliveredAt:firebase.database.ServerValue.TIMESTAMP})});if(!tx.committed)return false;await pr.child(order.partId).transaction(cur=>{if(!cur)return cur;const next=partQty(cur)+Number(order.quantity||0);return Object.assign({},cur,{quantity:next,stock:next,qty:next,quantidade:next,lastPurchaseAt:cur.lastPurchaseAt||firebase.database.ServerValue.TIMESTAMP,lastPurchaseQuantity:cur.lastPurchaseQuantity||Number(order.quantity||0),lastPurchaseStatus:'delivered',lastDeliveryAt:firebase.database.ServerValue.TIMESTAMP})});return true}
let pendingOrders={};
async function processDeliveries(){if(window.__partsDeliveryBusy)return;window.__partsDeliveryBusy=true;try{let changed=false;const orders=await loadOrders();for(const [id,o] of Object.entries(orders||{})){if(o&&o.status==='pending'&&Number(o.etaAt)<=Date.now()){if(await deliverOrder(id,o))changed=true}}if(changed)pendingOrders=await loadOrders();else pendingOrders=orders}catch(e){console.error('Erro ao processar entregas:',e)}finally{window.__partsDeliveryBusy=false}}
function deliveryFor(id){return Object.entries(pendingOrders||{}).filter(([oid,o])=>o&&o.partId===id&&o.status==='pending').map(([oid,o])=>({id:oid,o:o})).sort((a,b)=>Number(a.o.etaAt)-Number(b.o.etaAt))}
function renderDelivery(card,id){let box=card.querySelector('.parts-delivery-status');const list=deliveryFor(id);if(!list.length){if(box)box.remove();return}if(!box){box=document.createElement('div');box.className='parts-delivery-status';const buy=card.querySelector('.parts-buy-btn');if(buy&&buy.parentNode)buy.parentNode.insertBefore(box,buy);else card.appendChild(box)}const first=list[0].o;const total=list.reduce((sum,x)=>sum+Number(x.o.quantity||0),0);box.textContent='🚚 Entrega em '+formatRemaining(Number(first.etaAt)-Date.now())+' • '+total+' '+String(first.unit||'un')+' em trânsito'}
function buyUI(card,id,p){if(card.querySelector('.parts-buy-btn')){renderDelivery(card,id);return}const b=document.createElement('button');b.type='button';b.className='parts-buy-btn';b.innerHTML='<i class="fa-solid fa-cart-shopping" style="margin-right:7px"></i>Comprar Peça';b.addEventListener('click',async e=>{e.preventDefault();e.stopPropagation();let box=card.querySelector('.parts-buy-box');if(box){box.classList.toggle('open');return}box=document.createElement('div');box.className='parts-buy-box open';box.innerHTML='<div style="font-size:11px;color:#94a3b8;margin-bottom:6px">Quantidade a comprar</div><input class="pbb-input" type="number" min="1" step="1" value="1"><div style="font-size:11px;color:#94a3b8;margin-top:8px">Tempo de entrega: <strong style="color:#e2e8f0">30 segundos</strong></div><div class="pbb-actions"><button type="button" class="pbb-cancel">Cancelar</button><button type="button" class="pbb-confirm">Confirmar compra</button></div>';card.appendChild(box);box.querySelector('.pbb-cancel').onclick=()=>box.remove();box.querySelector('.pbb-confirm').onclick=async()=>{const q=Number(box.querySelector('.pbb-input').value);if(!Number.isInteger(q)||q<=0){alert('Informe uma quantidade inteira maior que zero.');return}const confirmBtn=box.querySelector('.pbb-confirm');confirmBtn.disabled=true;confirmBtn.textContent='Registrando...';try{await createPurchase(id,p,q);box.remove();await processDeliveries();pendingOrders=await loadOrders();renderDelivery(card,id);alert('Compra confirmada! '+q+' '+String(value(p,'unit','unidade')||'un')+' chegará ao almoxarifado em 30 segundos.')}catch(err){console.error('Erro ao registrar compra:',err);alert('Não foi possível registrar a compra no Firebase.');confirmBtn.disabled=false;confirmBtn.textContent='Confirmar compra'}}});card.appendChild(b);renderDelivery(card,id)}
async function addButtons(){const list=document.getElementById('partsList');if(!list)return;const parts=await loadParts();const cards=[...list.children].filter(x=>x&&x.nodeType===1);cards.forEach((card,i)=>{const ent=findEntry(card,parts,i);if(ent)buyUI(card,ent[0],ent[1]||{})})}
function scan(){addStyles();addButtons()}
function tick(){Object.keys(pendingOrders||{}).forEach(oid=>{const o=pendingOrders[oid];if(o&&o.status==='pending'&&Number(o.etaAt)<=Date.now())processDeliveries()});document.querySelectorAll('#partsList>*').forEach((card,i)=>{const text=(card.textContent||'').toLowerCase();const ent=entries(window.__partsLoaded||{}).find(([id,p])=>partName(p,id).toLowerCase()&&text.includes(partName(p,id).toLowerCase()));if(ent)renderDelivery(card,ent[0])});}
async function refresh(){pendingOrders=await loadOrders();await processDeliveries();pendingOrders=await loadOrders();scan()}
function init(){addStyles();refresh();const list=document.getElementById('partsList');if(list)new MutationObserver(()=>{clearTimeout(window.__partsBuyTimer);window.__partsBuyTimer=setTimeout(scan,80)}).observe(list,{childList:true,subtree:true});new MutationObserver(()=>{clearTimeout(window.__partsBuyBodyTimer);window.__partsBuyBodyTimer=setTimeout(scan,150)}).observe(document.body,{childList:true,subtree:true});setInterval(()=>{processDeliveries().then(()=>{pendingOrders=Object.assign({},pendingOrders);scan()})},1000);[300,800,1500,3000,5000,8000].forEach(ms=>setTimeout(scan,ms))}
window.buyInventoryPart=async function(id,p){const q=prompt('Quantas unidades de "'+partName(p,id)+'" deseja comprar?','1');if(q===null)return;const n=Number(q);if(!Number.isInteger(n)||n<=0)return alert('Informe uma quantidade inteira maior que zero.');try{await createPurchase(id,p,n);pendingOrders=await loadOrders();alert('Compra confirmada! A entrega chegará ao almoxarifado em 30 segundos.');}catch(e){console.error(e);alert('Não foi possível registrar a compra no Firebase.')}};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
